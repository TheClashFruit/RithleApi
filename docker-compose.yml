services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: rithle-backend
    restart: unless-stopped
    ports:
      - "${PORT:-3000}:${PORT:-3000}"
    env_file:
      - .env
    volumes:
      - ./data:/usr/src/app/data